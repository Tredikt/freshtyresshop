[tool.poetry]
name = "web-app"
version = "0.1.0"
description = "webapp_poetry"
authors = ["Tredikt <ivantkacev7810@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
